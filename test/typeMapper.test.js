import { expect } from 'chai';
import typeMapper from '../src/typeMapper';

import {
  BOOLEAN,
  ENUM,
  FLOAT,
  INTEGER,
  STRING
} from 'sequelize';

import {
  GraphQLString,
  GraphQLInt,
  GraphQLBoolean,
  GraphQLFloat,
  GraphQLEnumType,
  GraphQLList
} from 'graphql';

describe('typeMapper', () => {
  // TODO write tests for typeMapper
})
